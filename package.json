{
  "name": "telegram-bot",
  "version": "1.0.0",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm install --build-from-source=false",
    "heroku-postbuild": "npm install --prefer-offline --no-audit"
  },
"dependencies": {
  "node-telegram-bot-api": "^0.66.0",
  "pg": "^8.11.0"
},
  "engines": {
    "node": "18.x"
  }
}
